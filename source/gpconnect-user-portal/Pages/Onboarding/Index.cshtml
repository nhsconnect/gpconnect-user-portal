@page "{contentPage?}"
@model OnboardingModel

<h1>Onboarding with GP Connect</h1>

<div class="nhsuk-grid-row">
    <partial name="Links" />
    <div class="nhsuk-grid-column-two-thirds">
        <div class="nhsuk-card">
            <div class="nhsuk-card__content">
                <partial name="@Model.ContentPage" />
            </div>
        </div>
    </div>
</div>